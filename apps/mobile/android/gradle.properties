# UNVERIFIED: Android shell Gradle properties (task 8.5). Not built in CI.
org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official
# Path to the directory holding cyber_capi.h + libcyber_capi.so per ABI. Override
# on the command line (-PcyberCapiDir=...) or in a local gradle.properties once
# the engine is cross-compiled for Android.
cyberCapiDir=../../../../build-android
